#define string char*
#define print printf
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, string **argv) {
    string s[] = { "hello", "world" };
    char* p = s[0];
    p = s[1];
    (arr)char z = 0;
    printf("%s\n", z);
    printf("%s\n", p);
    return 0;
}
